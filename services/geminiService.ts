
import { fileToBase64, readFileAsText } from "./fileUtils";

// AI features have been disabled for the P2P version of AeroDrop.
// This service is kept as a placeholder to avoid breaking imports in other files.

export const analyzeFile = async (file: File): Promise<string> => {
  return "AI 分析功能已禁用。";
};
